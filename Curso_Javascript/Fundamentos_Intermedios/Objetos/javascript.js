/*const saludoId = document.getElementById('saludo');

function saludar(nombre) {
    return 'Hola ' + nombre;
}

const saludar = (nombre) => {
    return 'Hola ' + nombre;
}

saludoId.innerHTML = saludar("Manolo");

const sumaId = document.getElementById('suma');

const sumar = (num1,num2) => num1+num2;

sumaId.innerHTML += sumar(4,13);
*/

/* OBJETOS 

    CONSUMIR UNA API --> se manejan a través de URLs.
*/

//const arreglo = ['HTML', 15, true];

//console.log(arreglo)
/*
const objeto = {
    nombre: 'HTML',
    duracion: 15,
    estado: true,
    capitulos: {
        nombre: 'Introducción',
        videos: 20,
    }
}

console.log(objeto);
*/
/*
const arrayObjetos = [
    {
        nombre: 'HTML',
        estado: true
    },
    {
        nombre: 'CSS',
        estado: true
    },
    {
        nombre: 'JS',
        estado: false
    },
]

arrayObjetos.forEach(element => {
    console.log(element.nombre)
});*/

/* Fetch */

